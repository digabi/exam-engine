export const examTitleId = 'title'

export const referencesTitleId = 'references-title'

export const sectionTitleId = (displayNumber: string): string => `${displayNumber}-title`

export const questionTitleId = (displayNumber: string): string => `${displayNumber}-title`

export const tocTitleId = 'toc-title'
